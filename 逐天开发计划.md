# Aftersight / Afterscope — 逐天开发计划（35天）

> **项目**：电商智能售后中台  
> **后端**：Aftersight（Spring Boot 4 + MySQL + Redis + RabbitMQ + PgVector + LangChain4j）  
> **前端**：Afterscope（Vue 3 + Vue Router 4 + Axios + Element Plus + Chart.js）  
> **总工期**：35 天 · 6 个阶段 · 每日可交付

---

## 阶段总览

| 阶段 | 天数 | 内容 | 产出 |
|---|---|---|---|
| **一、基础设施搭建** | 第1-5天 | 项目配置、数据库建表、通用代码层 | 可运行的空壳应用 |
| **二、售后工单核心链路** | 第6-12天 | 工单CRUD、MQ异步、AI审核、人工审核 | 工单全流程跑通 |
| **三、RAG知识库与向量检索** | 第13-17天 | 文档管理、切片向量化、RAG检索、判例自迭代 | AI审核有知识支撑 |
| **四、运维支撑体系** | 第18-22天 | 死信运维、系统配置、仪表盘报表 | 后台完备 |
| **五、前端全面开发** | 第23-30天 | 7个页面全部实现、前后端联调 | 完整可用前端 |
| **六、集成测试与收尾** | 第31-35天 | 全链路测试、Bug修复、文档完善、部署 | 可演示的完整系统 |

---

## 阶段一：基础设施搭建（第1-5天）

### 第1天 — 项目初始化与依赖配置

**后端（aftersight）：**

- [ ] 完善 `pom.xml`，添加全部依赖：
  - `spring-boot-starter-web`（Web）
  - `spring-boot-starter-data-redis`（Redis）
  - `spring-boot-starter-amqp`（RabbitMQ）
  - `mybatis-spring-boot-starter` + `pagehelper-spring-boot-starter`（ORM + 分页）
  - `mysql-connector-j` + `postgresql`（双数据库驱动）
  - `langchain4j-spring-boot-starter` + `langchain4j-ollama`（AI）
  - `lombok` + `hutool-all` + `fastjson2`（工具）
  - `spring-boot-starter-validation`（参数校验）
  - `poi` + `pdfbox`（文档解析）
  - `spring-boot-starter-actuator`（监控）
- [ ] `application.yml` 分层配置：
  - `application-dev.yml`：开发环境（MySQL/Redis/RabbitMQ/PgVector 连接信息）
  - `application-prod.yml`：生产环境
  - 配置 MyBatis 映射路径、PageHelper 分页参数
- [ ] 创建 `com.aftersight` 基础包结构：
  ```
  com.aftersight
  ├── common/        # 统一响应体、全局异常处理、常量
  ├── config/        # Redis/RabbitMQ/CORS/MyBatis 配置类
  ├── controller/    # REST 控制器
  ├── service/       # 业务接口 + 实现
  ├── mapper/        # MyBatis Mapper 接口
  ├── entity/        # 数据库实体
  ├── dto/           # 请求/响应 DTO
  ├── enums/         # 枚举
  ├── mq/            # RabbitMQ 生产/消费
  └── ai/            # RAG + LLM
  ```
- [ ] 编写 `Result<T>` 统一响应体（code/message/data/timestamp）
- [ ] 编写 `GlobalExceptionHandler` 全局异常处理
- [ ] 编写 `PageResult<T>` 分页响应封装

**前端（afterscope）：**

- [ ] `npm install` 添加依赖：
  - `vue-router@4` + `axios` + `element-plus` + `@element-plus/icons-vue`
  - `chart.js` + `vue-chartjs`
  - `dayjs`（日期处理）
- [ ] 创建目录结构：
  ```
  src/
  ├── api/           # axios 接口封装
  ├── router/        # 路由配置
  ├── views/         # 页面组件
  ├── components/    # 公共组件
  ├── layout/        # 布局组件
  ├── utils/         # 工具函数
  └── assets/        # 静态资源
  ```
- [ ] 配置 `vue.config.js`：devServer proxy 代理到 `http://localhost:8080`
- [ ] 配置 `axios` 拦截器（统一请求头、错误处理）

**可交付检查：** `mvn spring-boot:run` 启动成功，`npm run serve` 启动成功

---

### 第2天 — 数据库建表 + 基础配置类

- [ ] 执行 `sql/init-mysql.sql`，确认 8 张表创建成功
- [ ] 验证种子数据：10家店铺 + 7条订单 + 7条工单 + 种子配置
- [ ] 执行 `sql/init-pgvector.sql`，确认 pgvector 扩展安装成功
- [ ] 验证种子数据：5份文档 + 18条切片 + 4个判例
- [ ] 测试 `search_similar_chunks()` 函数可用

**后端配置类：**

- [ ] `RedisConfig`：配置 `RedisTemplate<String, Object>`，序列化方式
- [ ] `RabbitMQConfig`：声明交换机、队列、绑定关系、死信交换机
- [ ] `CorsConfig`：跨域配置（允许前端 localhost:8081）
- [ ] `MyBatisConfig`：扫描 mapper 路径
- [ ] `RestTemplateConfig`：用于 Ollama LLM 调用

**可交付检查：** 数据库表齐全，配置类加载无报错

---

### 第3天 — 实体类 + 枚举 + Mapper 层

- [ ] 编写所有 `entity`（与 MySQL 8 张表对应）：
  - `StoreInfo` / `OrderInfo` / `AfterSaleOrder` / `AiAuditLog`
  - `DeadLetterMessage` / `SystemConfig` / `OperationLog` / `ReminderLog`
- [ ] 使用 Lombok：`@Data` `@TableName` `@TableId`
- [ ] 编写全部枚举类：
  - `AfterSaleTypeEnum`（1仅退款/2退货退款/3投诉）
  - `TicketStatusEnum`（0待AI审核/1AI已办结/2待人工审核/3已驳回/4已关闭）
  - `AiAuditStatusEnum`（0待审核/1已办结/2待人工判定）
  - `DlqStatusEnum`（0待处理/1已重试/2已删除）
  - `ConfigGroupEnum`（redis/ai_rag/mq）
  - `BizTypeEnum`（order_audit/doc_upload/dlq_retry/config_update）
- [ ] 编写所有 `mapper` 接口 + XML：
  - 基础 CRUD：`insert` / `selectById` / `updateById`
  - 分页查询：`selectPage` 带动态条件（`<where>` `<if>`）
  - Mapper XML 统一放在 `resources/mapper/` 下
- [ ] 编写 `AfterSaleOrderMapper` 复杂查询（6 个筛选条件组合）

**可交付检查：** 所有 Mapper 单测通过（可用 `@SpringBootTest` + H2 或直连 MySQL）

---

### 第4天 — 通用 Service 层 + DTO

- [ ] 编写所有 `service` 接口：
  - `AfterSaleOrderService` / `AiAuditLogService` / `DeadLetterService`
  - `KnowledgeBaseService` / `DashboardService` / `ReportService`
  - `SystemConfigService` / `OperationLogService` / `BasicDataService`
- [ ] 编写基础 `BaseService<T>` 抽象类（通用 CRUD + 分页）
- [ ] 编写 DTO 类（对应接口文档所有请求/响应结构）：
  - `SubmitOrderRequest` / `SubmitOrderResponse`
  - `OrderListQuery` / `OrderDetailResponse`
  - `ManualAuditRequest` / `ManualAuditResponse`
  - `BatchAssignRequest` / `BatchRetryRequest`
  - `DashboardStatsResponse` / `StoreRankingResponse`
  - `ConfigUpdateRequest`
  - 等 20+ DTO 类
- [ ] 使用 `@Valid` 注解标注参数校验规则

**可交付检查：** DTO 覆盖所有接口文档中的请求/响应

---

### 第5天 — 工单号生成器 + Redis 工具 + 消息工具

- [ ] 编写 `TicketNoGenerator`：格式 `SH + yyyyMMdd + 3位自增序列`
  - 方案：Redis `INCR` 生成每日序列号，key = `ticket:seq:20260605`
  - 兜底：数据库 `SELECT MAX(ticket_no)` 补位
- [ ] 编写 `RedisRateLimiter`：
  - 令牌桶算法实现（LUA 脚本保证原子性）
  - 布隆过滤器操作封装（`Redisson` 或手动实现）
  - 分布式锁工具 `RedisLock`（SET NX EX）
  - Redis ZSet 排行工具
- [ ] 编写 `RabbitMqProducer`：
  - `sendAfterSaleMessage(ticketNo, afterSaleType)` — 根据类型路由
  - `sendDocParseMessage(docId)` — 文档解析
  - `sendDelayReminderMessage(ticketNo, delayMillis)` — 延迟催单
- [ ] 编写 `OperationLogRecorder`（AOP 切面记录操作日志）

**可交付检查：** 组件单测覆盖：限流LUA脚本测试、工单号生成去重测试、MQ消息投递测试

---

## 阶段二：售后工单核心链路（第6-12天）

### 第6天 — 用户提交售后接口

- [ ] `AfterSaleController.submit()` 完整实现：
  1. 布隆过滤器校验订单号是否存在
  2. RedisRateLimiter 令牌桶限流检查
  3. 查询订单信息（Redis缓存优先）
  4. 生成工单号、写入 `after_sale_order` 表
  5. Redis ZINCRBY 更新店铺排行计数
  6. 写入 `cache:order:{orderNo}` 缓存
  7. RabbitMqProducer 投递审核消息
  8. 返回「提交成功，AI审核中」
- [ ] 参数校验：`@Valid` + 自定义校验器
- [ ] 全局异常情况处理（布隆校验失败 → 400；限流 → 429）

**可交付检查：** `POST /api/after-sale/submit` 接口调通

---

### 第7天 — 工单查询接口

- [ ] `AfterSaleController.list()` 分页查询：
  - MyBatis `PageHelper` 分页
  - 6 个动态查询条件（`<if>` 拼接）
  - 关联 `store_info` 表查出 `storeName`
- [ ] `AfterSaleController.detail(ticketNo)` 详情查询：
  - 查询 `after_sale_order` 基本字段
  - 缓存优先查 `cache:order:{orderNo}` 获取商品信息
  - 解析 `rag_rule_ids` → 查 `rag_chunk` 获取相似度 + 原文
  - 查 `ai_audit_log` 获取 LLM 原始输出
- [ ] `AfterSaleController.export()` Excel 导出：
  - 使用 Apache POI 生成 `.xlsx`
  - 响应流下载

**可交付检查：** 列表/详情/导出 三个接口可用

---

### 第8天 — MQ 消费者基础 + AI 审核框架

- [ ] 编写 `AfterSaleConsumer`（MQ 消息监听）：
  - `@RabbitListener` 监听 `queue.refund.only` / `queue.refund.return` / `queue.complaint`
  - 幂等校验：查 `ticket_status` 已处理则直接 ACK
  - Redis 分布式锁：`lock:order:audit:{ticketNo}`
  - 调用 `AiAuditService.audit(ticketNo)`
  - 成功 → 更新工单；失败 → nack + 重试
- [ ] 编写 `AiAuditService.audit()` 骨架：
  1. 查询工单信息
  2. 调用 `RagRetrievalService.retrieve()` （暂返回空列表，下天实现）
  3. 调用 `LlmService.invoke()` （暂返回模拟结果，下天实现）
  4. 解析 LLM 结果、判定工单状态
  5. 写入 `ai_audit_log`
  6. 更新 `after_sale_order`
- [ ] 配置死信机制：
  - 消费者抛出异常 → 重试 3 次 → 进入死信队列

**可交付检查：** MQ 消费流程跑通，AI 审核骨架可执行

---

### 第9天 — RAG 检索服务

- [ ] 编写 `RagRetrievalService`：
  - `retrieve(queryText, afterSaleType, topN)` → 召回切片列表
  - 调用 Embedding 模型（先 `HttpClient` 调用 Ollama Embedding API）
  - 拼装 SQL：`SELECT ... ORDER BY embedding <=> ? LIMIT ?`
  - 使用 `JdbcTemplate` 或 MyBatis 执行向量检索
  - 返回：`RagChunkDTO`（含切片ID、原文、相似度、文档名）
- [ ] 编写 `search_combined_knowledge()` 的 JDBC 调用，同时检索 `rag_chunk` + `quality_case`
- [ ] 熔断处理：PgVector 不可用时跳过 RAG，LLM 裸审

**可交付检查：** 传入模拟文本，返回正确的 TopN 召回结果

---

### 第10天 — LLM 审核服务 + Prompt 工程

- [ ] 编写 `LlmService`：
  - `invoke(prompt)` → 调用 Ollama API（`http://{baseUrl}/api/chat`）
  - 使用 `RestTemplate` / `WebClient`，超时 30s
  - 支持流式/非流式两种模式
- [ ] Prompt 模板设计：
  ```
  你是电商售后审核专家。
  参考规则：
  [#1] {rule1}（相似度{s1}%）
  [#2] {rule2}（相似度{s2}%）
  
  售后申请：商品【{productName}】 原因【{reason}】 类型【{type}】
  
  请严格按照 JSON 格式输出：
  {"conclusion":"同意退款/驳回/待人工","confidence":xx,"reason":"...","suggestedAction":"..."}
  ```
- [ ] `LlmResponseParser`：解析 LLM 返回的 JSON，处理异常格式
- [ ] `AiAuditService` 完整集成 RAG + LLM：
  - RAG 空 → 跳过 RAG，仅 LLM 判断
  - LLM 返回异常 → 降级为「待人工审核」
  - 置信度 ≥ 85% 直接办结；60-85% 待人工；<60% 待人工审核

**可交付检查：** LLM 可返回有效审核结论，全流程 Aftersight → RAG → LLM 可执行

---

### 第11天 — 人工审核 + 判例入库

- [ ] `AfterSaleController.manualAudit()` 实现：
  1. 接收 `ManualAuditRequest`
  2. 校验工单当前状态必须为「待人工审核」
  3. 更新 `after_sale_order`（ticket_status / manual_result / manual_remark / manual_audit_by / manual_audit_time）
  4. 记录 `operation_log`
- [ ] 判例异步入库 `QualityCaseService.buildCase(ticketNo)`：
  1. 查询工单详情 + AI 审核日志
  2. 组装判例标题、正文（`case_content`）
  3. 调用 Embedding 生成向量
  4. 写入 PostgreSQL `quality_case` 表
  5. 标记 `caseGenerated=true` 返回前端
- [ ] 批量指派接口 `batchAssign()` 实现

**可交付检查：** 人工审核 → 判例入库 → 后续 RAG 能召回该判例

---

### 第12天 — 延迟催单 + Redis 统计更新

- [ ] 延迟消息发送（AI 审核判定「待人工」时触发）：
  - 投递到 `exchange.delay`，TTL = `timeout.manual_review_hours` × 3600000ms
- [ ] `DelayReminderConsumer` 延迟消息消费者：
  - 查询工单状态是否为「待人工审核」
  - 是 → 记录 `reminder_log`（reminder_status=0）
  - 否 → 直接 ACK（幂等）
- [ ] Redis 统计更新（工单状态变更时触发）：
  - `cache:dashboard:stats` 增量更新
  - 日/月统计计数缓存
- [ ] 工单状态变更统一事件处理：
  - `@EventListener` 监听 `TicketStatusChangeEvent`
  - 同步触发：日志记录、Redis更新、排行榜更新

**可交付检查：** 待人工工单 24h 后催单记录生成

---

## 阶段三：RAG知识库与向量检索（第13-17天）

### 第13天 — 知识库文档上传

- [ ] `KnowledgeController.upload()` 实现：
  - `multipart/form-data` 接收文件 + 分类
  - 校验：格式(PDF/Word/TXT)、大小(≤20MB)
  - 保存文件到 `/{storage.path}/{docCode}_{originalName}`
  - 写入 `rag_document` 表（vectorize_status=0）
  - MQ 投递 `doc.parse` 消息
  - 返回「已进入解析向量化队列」
- [ ] `KnowledgeController.list()` 分页查询
- [ ] `KnowledgeController.delete(docId)` 删除：
  - 删除磁盘文件
  - 数据库级联删除切片（`ON DELETE CASCADE`）

**可交付检查：** 上传 PDF/TXT → 写入 DB → 投递 MQ

---

### 第14天 — 文档异步解析 + 切片

- [ ] `DocParseConsumer` 消费者：
  1. 接收 MQ 消息，获取 `docId` + `filePath`
  2. 更新 `vectorize_status=1`（解析中）
  3. 根据 `fileType` 选择解析器：
     - PDF → `PdfBoxTextExtractor`
     - DOCX → `PoiWordExtractor`
     - TXT → 直接读取
- [ ] 文本切片器 `TextChunker`：
  - 按 `chunk.size`（系统参数配置，默认512字符）分割
  - 智能切分：在段落/句子边界处断开，避免单词截断
  - 记录每个切片的 `token_count`
  - 批量写入 `rag_chunk` 表（embedding 字段暂空）

**可交付检查：** PDF 文件上传 → 解析出正确切片文本

---

### 第15天 — 向量化入库

- [ ] `VectorizeService.vectorizeChunks(docId)`：
  - 查询 `rag_chunk` where `doc_id = ? AND embedding IS NULL`
  - 分批调用 Embedding 模型（Ollama Embedding API）
  - UPDATE 写入向量字段
- [ ] 优化：批量处理，每批 10 条，控制并发数
- [ ] 完成后更新 `rag_document.vectorize_status=2`（已向量化）
- [ ] 失败处理：记录 `vectorize_error`，状态=3
- [ ] `KnowledgeController.chunks(docId)` 切片详情查询接口
- [ ] `KnowledgeController.reVectorize(docId)` 重新向量化接口

**可交付检查：** 切片向量化完成，向量检索可召回

---

### 第16天 — 前端 RAG 调试页面 + 相似度可视化

- [ ] `KnowledgeController.chunks(docId)` 返回完整切片列表
- [ ] 前端「切片详情弹窗」绑定数据
- [ ] 前端「上传文档弹窗」联调
- [ ] 工单详情弹窗中 RAG 召回规则展示（3条原文 + 相似度）

**可交付检查：** 知识库 CRUD 界面完整可用

---

### 第17天 — 判例自迭代完善 + 测试

- [ ] `QualityCaseService` 完整实现：
  - `buildCase()`：组装判例 → 向量化 → 入库
  - `deactivateCase(id)`：软删除（`is_active=false`）
- [ ] 联合检索验证：`search_combined_knowledge()` 同时召回规则+判例
- [ ] 验证自迭代闭环：
  1. 提交工单 → AI 审核「待人工」
  2. 人工同意 → 判例入库
  3. 再提交类似工单 → RAG 能召回新判例
- [ ] 判例去重：同工单号不可重复入库

**可交付检查：** 自迭代闭环端到端验证通过

---

## 阶段四：运维支撑体系（第18-22天）

### 第18天 — MQ 死信运维接口

- [ ] `DlqController.list()` 死信分页查询（按时间/工单号/异常原因筛选）
- [ ] `DlqController.retry(id)` 单条重试：
  1. 读取 `dead_letter_message` 原始消息内容
  2. 重新投递到原队列（`queueName`）
  3. 更新 `dlq_status=1`、`last_retry_time`
- [ ] `DlqController.batchRetry(ids)` 批量重试：
  1. 逐条执行重试逻辑
  2. 返回成功/失败计数
- [ ] `DlqController.delete(id)` 删除
- [ ] 死信自动入库消费者：监听死信队列，自动写入 `dead_letter_message` 表

**可交付检查：** 死信列表展示 + 重试后工单恢复审核

---

### 第19天 — 系统配置接口 + 热加载

- [ ] `ConfigController.list(group)` 按分组查询配置
- [ ] `ConfigController.update()` 更新单条配置：
  1. 更新 `system_config` 表
  2. 清除 Redis `cache:config:all` 对应 Hash 字段
  3. 记录 `operation_log`
- [ ] `ConfigController.batchUpdate()` 批量更新
- [ ] `ConfigController.reset(id)` 恢复默认值
- [ ] 配置读取工具 `SystemConfigServiceHolder`：
  - 业务代码统一通过 `SystemConfigService.getXxx(key, defaultVal)` 读取
  - Redis Hash → Miss 查 DB → 回写 Redis TTL 30s

**可交付检查：** 改配置 → 即刻生效 → 业务代码读到新值

---

### 第20天 — 仪表盘接口

- [ ] `DashboardController.stats()` 统计卡片：
  1. `cache:dashboard:stats` 命中直接返回
  2. Miss：聚合查询 今日新增/AI办结/待人工/通过率/死信数
  3. 计算环比（今日 vs 昨日）
  4. 回写 Redis TTL 60s
- [ ] `DashboardController.trend(days)` 折线图数据：
  - 按天 GROUP BY 查询 `after_sale_order`
- [ ] `DashboardController.typeRatio()` 售后类型饼图：
  - GROUP BY `after_sale_type`
- [ ] `DashboardController.storeRanking()` TOP10：
  - `ZREVRANGE rank:store:monthly:202606 0 9 WITHSCORES`
  - Miss 后查 DB 回写 ZSet

**可交付检查：** 4 个仪表盘接口返回正确数据

---

### 第21天 — 数据统计报表接口

- [ ] `ReportController.monthlySummary(month)` 月度总览
- [ ] `ReportController.daily(month)` 每日明细
- [ ] `ReportController.storeRanking(month, page, size)` 店铺排行
- [ ] `ReportController.rateTrend(months)` 通过率/驳回率趋势
- [ ] `ReportController.export(type, month)` 报表导出 Excel
- [ ] 报表数据均走 DB 实时查询（不需要缓存）

**可交付检查：** 5 个统计接口数据正确

---

### 第22天 — 基础数据接口 + 操作日志

- [ ] `BasicDataController.stores(keyword)` 店铺列表
- [ ] `BasicDataController.order(orderNo)` 订单查询
- [ ] `BasicDataController.afterSaleTypes()` 售后类型枚举
- [ ] `BasicDataController.ticketStatuses()` 工单状态枚举
- [ ] `OperationLogController.list(page, size, bizType, bizId)` 操作日志
- [ ] AOP 操作日志切面完善：
  - `@OperationLog(bizType, action)` 注解
  - 自动记录 Controller 请求参数、操作人、IP

**可交付检查：** 后端 34 个接口全部完成

---

## 阶段五：前端全面开发（第23-30天）

### 第23天 — 前端布局 + 路由 + 数据层

- [ ] 布局组件 `Layout.vue`：
  - 侧边栏：菜单高亮、折叠功能
  - 顶部栏：面包屑 + 用户信息
  - 内容区：`<router-view>`
- [ ] 路由配置 `router/index.js`：
  ```js
  / → redirect → /dashboard
  /dashboard          → Dashboard.vue
  /orders             → OrderList.vue
  /orders/detail/:no  → OrderDetail (弹窗)
  /knowledge          → KnowledgeBase.vue
  /dlq                → DlqList.vue
  /report             → ReportPage.vue
  /config             → ConfigPage.vue
  /user/apply         → UserApply.vue
  ```
- [ ] API 层 `api/`：
  - `afterSale.js` / `knowledge.js` / `dlq.js`
  - `dashboard.js` / `report.js` / `config.js` / `basic.js`
  - 统一封装：`request(config)` 自动处理 loading/error

**可交付检查：** 路由跳转正常，侧边栏可切换页面

---

### 第24天 — 首页仪表盘页面

- [ ] 统计卡片区域：5 个卡片组件 `StatsCard.vue`
- [ ] 折线图：Chart.js 封装 `LineChart.vue`（近7日趋势）
- [ ] 饼图：`DoughnutChart.vue`（售后类型占比）
- [ ] TOP10 排行榜组件：`StoreRanking.vue`（ZSet 实时排行）
- [ ] 快捷操作区：3 个快捷卡片链接到对应页面
- [ ] 数据绑定：`dashboard.js` → `Dashboard.vue`

**可交付检查：** 仪表盘页面与 HTML 原型一致

---

### 第25天 — 售后工单管理页面（列表）

- [ ] 筛选条件区域：
  - 订单号 input / 手机号 input / 店铺下拉 / 类型下拉 / 状态下拉 / 日期范围
  - 查询/重置/导出/批量指派/批量重试 按钮
- [ ] 工单表格：`el-table` 展示全部字段
  - 状态列：`el-tag` 不同颜色
  - AI审核结果列展示
  - 操作列：「详情」链接、「处理/驳回」按钮
- [ ] 分页组件：`el-pagination`
- [ ] 筛选联调：切换条件 → 重新请求 → 刷新表格

**可交付检查：** 工单列表查询、筛选、分页、导出可用

---

### 第26天 — 工单详情弹窗 + 人工审核

- [ ] 工单详情弹窗 `OrderDetailDialog.vue`：
  - 订单基础信息区（4 个字段）
  - 用户售后信息区
  - RAG 检索依据区（3 条规则，相似度标签）
  - AI 审核详情区（结论 + 置信度 + 理由）
  - 人工操作区：
    - 审核结果：`el-radio-group`（同意/驳回）
    - 备注：`el-input` textarea
    - 提交按钮
- [ ] 提交人工审核弹窗逻辑：
  - 显示 loading
  - 成功后提示「判例已自动入库」
  - 关闭弹窗、刷新列表
- [ ] 批量指派对话框

**可交付检查：** 详情弹窗 + 人工审核 + 判例入库全流程

---

### 第27天 — AI知识库管理页面

- [ ] 文档搜索 + 上传按钮区域
- [ ] 上传文档弹窗 `UploadDialog.vue`：
  - `el-upload` 文件选择
  - 分类下拉选择
  - 提交后提示「已进入异步解析队列」
- [ ] 文档列表表格：ID/名称/分类/大小/切片数/状态/操作
  - 操作：查看切片 / 重新向量化 / 删除
  - 向量化状态：`el-tag` 颜色区分
- [ ] 切片详情弹窗 `ChunkDialog.vue`：
  - 展示所有 Chunk，`chunk_index` + `chunk_text`

**可交付检查：** 知识库上传/列表/切片查看全部可用

---

### 第28天 — MQ死信运维 + 数据统计页面

- [ ] **死信页面 `DlqList.vue`：**
  - 筛选条件：时间区间、工单号、异常原因搜索
  - 死信表格：消息ID/队列/工单/异常原因/时间
  - 复选框 + 批量重试按钮
  - 操作列：重试 / 删除
- [ ] **统计页面 `ReportPage.vue`：**
  - 顶部 4 个统计卡片（月度总览）
  - 每日售后单数柱线混合图（Chart.js 混合图表）
  - 驳回率/通过率趋势折线图
  - 店铺排行表格
  - 导出报表按钮

**可交付检查：** 死信运维 + 统计报表页面可用

---

### 第29天 — 系统配置 + 用户端页面

- [ ] **系统配置页面 `ConfigPage.vue`：**
  - 按分组展示：Redis 限流 / AI RAG / MQ 延迟
  - 每个配置项：字段名 + input + 默认值提示
  - 保存配置按钮 + 恢复默认按钮
- [ ] **用户端页面 `UserApply.vue`：**
  - 订单号输入
  - 售后类型下拉
  - 申请原因 textarea
  - 凭证上传：拖拽/点击上传（`el-upload`）
  - 提交按钮
  - 提交成功后的反馈提示

**可交付检查：** 配置修改生效、售后申请可提交

---

### 第30天 — 前端联调 + 交互优化

- [ ] 前后端全量联调（34 个接口逐一验证）：
  - 请求正常发送
  - 响应正确展示
  - 异常情况（400/429/500）前端提示合理
- [ ] 交互优化：
  - 全局 loading 状态（axios 拦截器 + NProgress）
  - 空数据状态展示 `el-empty`
  - 错误提示 `ElMessage.error`
  - 表格行 `hover` 高亮
  - 页面切换动画
- [ ] 前端打包测试：`npm run build` 无报错

**可交付检查：** 前端页面完整可用，无报错无警告

---

## 阶段六：集成测试与收尾（第31-35天）

### 第31天 — 全链路主流程测试

- [ ] **场景 1：用户提交 → AI办结**
  1. 用户提交「仅退款」申请
  2. 限流拦截验证
  3. MQ 投递 → 消费者消费
  4. RAG 检索召回规则
  5. LLM 裁决 → 置信度≥85%
  6. 工单状态「AI已办结」
  7. 仪表盘统计更新
  8. 店铺排行 ZSet 更新

- [ ] **场景 2：AI待人工 → 人工处理 → 判例入库**
  1. 用户提交复杂售后（置信度<85%）
  2. AI 判定「待人工审核」
  3. 24h 无处理 → 催单记录生成
  4. 运营查看 RAG 溯源 → 人工审核同意
  5. 判例自动入库 → 向量化
  6. 后续同类申请可召回此判例

- [ ] **场景 3：异常链路 → 死信 → 重试**
  1. Ollama 超时 → 重试 3 次 → 死信
  2. 死信表写入
  3. 运维查看死信 → 点击重试
  4. 消息重新投递 → AI审核成功

**可交付检查：** 三大核心场景端到端验证通过

---

### 第32天 — 异常场景测试 + 容错验证

- [ ] Redis 宕机测试：
  - 限流降级（跳过限流）
  - 缓存 miss → 查 DB
  - 排行接口返回「暂不可用」
- [ ] RabbitMQ 宕机测试：
  - 工单创建成功但未投递 → 定时任务补偿
  - 页面显示「待重试」
- [ ] PgVector 宕机测试：
  - RAG 跳过 → LLM 裸审
  - AI 审核日志备注「RAG不可用」
- [ ] Ollama 不可用测试：
  - LLM 调用超时 → 重试 → 死信
  - 死信重试后恢复正常
- [ ] 参数校验测试：
  - 空订单号 → 400
  - 超长原因 → 400
  - 重复提交 → 幂等

**可交付检查：** 异常场景均有合理降级表现

---

### 第33天 — 性能测试 + 优化

- [ ] 接口压测（JMeter / Locust）：
  - `POST /api/after-sale/submit`：目标 500 QPS
  - `GET /api/after-sale/list`：目标 1000 QPS
  - `GET /api/dashboard/stats`：目标 2000 QPS
- [ ] 瓶颈分析：
  - Redis 缓存命中率监控
  - MQ 消息积压监控
  - DB 慢查询日志分析（`slow_query_log`）
- [ ] 优化：
  - 慢 SQL 加索引
  - 缓存 TTL 调整
  - MQ prefetch 数调整

**可交付检查：** 核心接口达到性能目标

---

### 第34天 — 文档补全 + 部署配置

- [ ] 补全 `README.md`：
  - 项目介绍 + 技术栈
  - 快速启动指南（本地开发）
  - 数据库初始化步骤
  - 环境要求（Java 17 / Node 18 / Redis / RabbitMQ / PostgreSQL）
- [ ] 编写 `Dockerfile`：
  - 后端：`maven:3.9-eclipse-temurin-17` 构建 → `eclipse-temurin:17-jre` 运行
  - 前端：`node:18` 构建 → `nginx:alpine` 运行
- [ ] 编写 `docker-compose.yml`：
  ```
  services:
    mysql / redis / rabbitmq / postgresql（含 pgvector）
    aftersight（后端）
    afterscope（前端）
  ```
- [ ] 配置 Spring Boot Profile：`application-prod.yml`

**可交付检查：** `docker-compose up` 一键启动全栈

---

### 第35天 — 最终验收 + 演示准备

- [ ] 全功能验收清单逐项确认：

  | 模块 | 功能 | 状态 |
  |---|---|---|
  | 仪表盘 | 5 个统计卡片 + 3 个图表 + 快捷操作 | □ |
  | 工单管理 | 筛选/列表/详情/人工审核/导出/批量操作 | □ |
  | AI审核链路 | RAG检索溯源 + LLM裁决 + 置信度 | □ |
  | 知识库 | 上传/解析/切片/向量化/查看切片 | □ |
  | 死信运维 | 列表/重试/批量重试/删除 | □ |
  | 统计报表 | 月度总览/趋势/排行/导出 | □ |
  | 系统配置 | Redis/AI/MQ 三组参数动态修改 | □ |
  | 用户端 | 售后申请提交 + MQ 异步处理 | □ |
  | 异常容错 | 限流/降级/重试/幂等/死信 | □ |

- [ ] 演示数据准备：
  - 足够丰富的工单数据（覆盖全状态）
  - 知识库文档 + 切片完整
  - 几条死信记录用于演示运维
- [ ] 录制演示视频 / 准备演示脚本

**可交付检查：** 全部验收项通过，系统可演示

---

## 快速参考

### 每日时间分配建议

```
上午（3h）：编码开发
下午（3h）：编码开发  
晚上（1h）：测试验证 + 写日报
```

### 关键里程碑

| 天 | 里程碑 | 验收标准 |
|---|---|---|
| 第5天 | 基础设施就绪 | 空壳应用启动 + 数据库就绪 |
| 第12天 | 工单核心链路跑通 | 提交→MQ→AI→人工→判例 全流程 |
| 第17天 | RAG 知识库就绪 | 上传→解析→向量→检索 闭环 |
| 第22天 | 后端 34 个接口全部完成 | 接口文档逐条验收 |
| 第30天 | 前端 7 个页面全部完成 | 与 HTML 原型一致 |
| 第35天 | 系统可演示 | 全链路验收通过 |

### 风险预警

| 风险 | 影响 | 应对 |
|---|---|---|
| Ollama 无法本地部署 | AI审核不可用 | 先用模拟 LLM 返回值开发，后续接入任何 OpenAI 兼容 API |
| pgvector 安装困难 | 向量检索不可用 | 先用 `pgvector` Docker 镜像，本地环境用 H2 模拟 |
| RabbitMQ 配置复杂 | MQ 链路不通 | 先用 Docker 一键启动，开发期可用 `SimpleMessageConverter` 调试 |
| 工期不够 | 部分页面未完成 | 优先保证核心链路（工单 + AI），报表/配置页面可后移 |
