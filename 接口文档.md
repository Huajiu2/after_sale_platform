# 电商智能售后中台 — API 接口文档

> 基础路径：`/api` · 响应格式统一为 JSON · 分页参数统一为 `?page=1&size=20`

---

## 目录

1. [通用说明](#一通用说明)
2. [售后工单接口](#二售后工单接口)
3. [AI知识库接口](#三ai知识库接口)
4. [MQ死信运维接口](#四mq死信运维接口)
5. [数据仪表盘接口](#五数据仪表盘接口)
6. [数据统计接口](#六数据统计接口)
7. [系统配置接口](#七系统配置接口)
8. [基础数据接口](#八基础数据接口)
9. [操作日志接口](#九操作日志接口)

---

| 模块           | 接口数 | 核心接口                                                     |
| -------------- | ------ | ------------------------------------------------------------ |
| **售后工单**   | 7个    | 提交申请、列表查询、详情查询、人工审核、批量指派、批量重试、导出Excel |
| **AI知识库**   | 5个    | 文档列表、上传文档（multipart）、切片详情、重新向量化、删除文档 |
| **MQ死信运维** | 4个    | 死信列表、单条重试、批量重试、删除死信                       |
| **数据仪表盘** | 4个    | 统计卡片、近7日趋势、类型占比、店铺TOP10                     |
| **数据统计**   | 5个    | 月度总览、每日明细、店铺排行、通过率趋势、导出报表           |
| **系统配置**   | 4个    | 配置列表（按分组）、更新配置、批量更新、恢复默认             |
| **基础数据**   | 4个    | 店铺列表、订单查询、售后类型枚举、工单状态枚举               |
| **操作日志**   | 1个    | 操作日志列表                                                 |

---

| # | 模块 | 方法 | 路径 | 说明 |
|---|---|---|---|---|
| 1 | 工单 | POST | `/api/after-sale/submit` | 提交售后申请（multipart 上传图片）✅ |
| 2 | 工单 | GET | `/api/after-sale/list` | 工单列表（分页）   ✅ |
| 3 | 工单 | GET | `/api/after-sale/detail/{ticketNo}` | 工单详情 |
| 4 | 工单 | POST | `/api/after-sale/manual-audit` | 人工审核提交 |
| 5 | 工单 | POST | `/api/after-sale/batch-assign` | 批量指派客服 |
| 6 | 工单 | POST | `/api/after-sale/batch-retry` | 批量重试工单 |
| 7 | 工单 | GET | `/api/after-sale/export` | 导出Excel |
| 8 | 知识库 | GET | `/api/knowledge/list` | 文档列表（分页） |
| 9 | 知识库 | POST | `/api/knowledge/upload` | 上传文档 |
| 10 | 知识库 | GET | `/api/knowledge/chunks/{docId}` | 切片详情 |
| 11 | 知识库 | POST | `/api/knowledge/re-vectorize/{docId}` | 重新向量化 |
| 12 | 知识库 | DELETE | `/api/knowledge/{docId}` | 删除文档 |
| 13 | 死信 | GET | `/api/dlq/list` | 死信列表（分页） |
| 14 | 死信 | POST | `/api/dlq/retry/{id}` | 单条重试 |
| 15 | 死信 | POST | `/api/dlq/batch-retry` | 批量重试 |
| 16 | 死信 | DELETE | `/api/dlq/{id}` | 删除死信 |
| 17 | 仪表盘 | GET | `/api/dashboard/stats` | 仪表盘统计 ✅ |
| 18 | 仪表盘 | GET | `/api/dashboard/trend` | 近7日趋势 ✅ |
| 19 | 仪表盘 | GET | `/api/dashboard/type-ratio` | 类型占比 ✅ |
| 20 | 仪表盘 | GET | `/api/dashboard/store-ranking` | 店铺TOP10 ✅ |
| 21 | 统计 | GET | `/api/report/monthly-summary` | 月度总览 |
| 22 | 统计 | GET | `/api/report/daily` | 每日明细 |
| 23 | 统计 | GET | `/api/report/store-ranking` | 店铺排行 |
| 24 | 统计 | GET | `/api/report/rate-trend` | 通过率趋势 |
| 25 | 统计 | GET | `/api/report/export` | 导出报表 |
| 26 | 配置 | GET | `/api/config/list` | 配置列表 |
| 27 | 配置 | PUT | `/api/config/update` | 更新配置 |
| 28 | 配置 | PUT | `/api/config/batch-update` | 批量更新 |
| 29 | 配置 | POST | `/api/config/reset/{id}` | 恢复默认 |
| 30 | 基础 | GET | `/api/basic/stores` | 店铺列表 |
| 31 | 基础 | GET | `/api/basic/order/{orderNo}` | 订单查询 |
| 32 | 基础 | GET | `/api/basic/after-sale-types` | 售后类型枚举 |
| 33 | 基础 | GET | `/api/basic/ticket-statuses` | 工单状态枚举 |
| 34 | 日志 | GET | `/api/log/list` | 操作日志 |

---

## 一、通用说明

### 1.1 统一响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {},
  "timestamp": 1717545600000
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| `code` | int | 业务状态码：200 成功 / 400 参数错误 / 429 限流 / 500 服务端异常 |
| `message` | string | 提示信息 |
| `data` | object/array | 响应数据 |
| `timestamp` | long | 服务器时间戳 |

### 1.2 分页响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [],
    "total": 156,
    "page": 1,
    "size": 20,
    "pages": 8
  }
}
```

### 1.3 全局状态码

| Code | 含义 | 说明 |
|---|---|---|
| 200 | 成功 | 请求正常处理 |
| 400 | 参数错误 | 请求参数校验失败 |
| 401 | 未认证 | 需要登录 |
| 403 | 无权限 | 权限不足 |
| 404 | 资源不存在 | 请求的资源未找到 |
| 429 | 请求限流 | Redis 令牌桶限流触发 |
| 500 | 服务端异常 | 系统内部错误 |

---

## 二、售后工单接口

### 2.1 用户提交售后申请

用户在页面点击“提交”时，一次性完成文件上传、OSS 存储、工单创建和后续 AI 审核。

```
POST /api/after-sale/submit
```

**Request：** `multipart/form-data`

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `orderNo` | string | 是 | 订单号 |
| `afterSaleType` | int | 是 | 售后类型：1仅退款 2退货退款 3投诉 |
| `applyReason` | string | 是 | 申请原因，0~500字 |
| `evidenceFiles` | file[] | 否 | 凭证图片文件，支持 jpg/jpeg/png/gif，最多 5 张 |

**处理流程：**

1. 接收前端提交的表单字段和图片文件
2. 将图片上传到阿里云 OSS
3. 获取 OSS 返回的图片 URL
4. 将 URL 列表写入数据库 `after_sale_order.evidence_images`
5. 创建售后工单并返回结果

**Response：**

```json
{
  "code": 200,
  "message": "售后申请提交成功，AI审核中",
  "data": {
    "ticketNo": "SH20260605001",
    "ticketStatus": 0,
    "estimatedTime": "30秒内出结果",
    "message": "售后申请提交成功"
  }
}
```

**异常响应 — 限流触发：**

```json
{
  "code": 429,
  "message": "提交过于频繁，请 45 秒后再试",
  "data": {
    "retryAfterSeconds": 45
  }
}
```

---

### 2.2 工单列表查询（分页）

运营后台工单管理页的主列表查询。

```
GET /api/after-sale/list?page=1&size=20
```

**Query Parameters：**

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `page` | int | 否 | 页码，默认1 |
| `size` | int | 否 | 每页条数，默认20 |
| `orderNo` | string | 否 | 订单号（精准匹配） |
| `userPhone` | string | 否 | 用户手机号 |
| `storeId` | long | 否 | 店铺ID |
| `afterSaleType` | int | 否 | 售后类型：1仅退款 2退货退款 3投诉 |
| `ticketStatus` | int | 否 | 工单状态：0待AI审核 1AI已办结 2待人工审核 3已驳回 4已关闭 |
| `startTime` | string | 否 | 创建时间起始，格式 yyyy-MM-dd |
| `endTime` | string | 否 | 创建时间结束，格式 yyyy-MM-dd |

**Response：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "ticketNo": "SH20260605001",
        "orderNo": "DD998765",
        "storeName": "XX数码旗舰店",
        "afterSaleType": 1,
        "afterSaleTypeDesc": "仅退款",
        "applyReason": "商品屏幕碎裂",
        "hasEvidence": true,
        "aiAuditResult": "同意退款",
        "aiConfidence": 94.30,
        "ticketStatus": 1,
        "ticketStatusDesc": "AI已办结",
        "createdAt": "2026-06-05 10:20:00"
      }
    ],
    "total": 156,
    "page": 1,
    "size": 20,
    "pages": 8
  }
}
```

---

### 2.3 工单详情查询

```
GET /api/after-sale/detail/{ticketNo}
```

**Path Parameters：**

| 参数名 | 类型 | 说明 |
|---|---|---|
| `ticketNo` | string | 工单号，如 SH20260605001 |

**Response：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "ticketNo": "SH20260605001",
    "orderInfo": {
      "orderNo": "DD998765",
      "productName": "iPhone 15 Pro Max 256GB",
      "productSpec": "深黑色 / 256GB",
      "payAmount": 8999.00,
      "orderTime": "2026-06-03 14:30:22",
      "storeName": "XX数码旗舰店"
    },
    "afterSaleInfo": {
      "afterSaleType": 1,
      "afterSaleTypeDesc": "仅退款",
      "applyReason": "商品屏幕碎裂",
      "evidenceImages": [
        "https://oss.example.com/img01.jpg",
        "https://oss.example.com/img02.jpg"
      ],
      "applyAmount": 8999.00
    },
    "ragEvidence": [
      {
        "rank": 1,
        "similarity": 96.80,
        "ruleContent": "【平台售后规则第3章第2条】买家签收后7日内发现商品存在性能故障或外观损坏...",
        "sourceDoc": "平台7天无理由售后规则.pdf",
        "chunkId": 12
      },
      {
        "rank": 2,
        "similarity": 91.20,
        "ruleContent": "【数码3C售后规范第5条】对于屏幕碎裂类投诉...",
        "sourceDoc": "数码3C售后处理规范.docx",
        "chunkId": 8
      },
      {
        "rank": 3,
        "similarity": 85.70,
        "ruleContent": "【历史判例 ID:CASE0428】类似碎屏案例：经AI图像识别检查...",
        "sourceDoc": "判例库",
        "chunkId": 5
      }
    ],
    "aiAuditDetail": {
      "conclusion": "同意全额退款",
      "confidence": 94.30,
      "reason": "用户上传凭证显示屏幕碎裂无外力撞击痕迹...",
      "suggestedAction": "全额退款",
      "modelName": "qwen2.5:7b",
      "latencyMs": 2846,
      "auditTime": "2026-06-05 10:20:15"
    },
    "ticketStatus": 1,
    "ticketStatusDesc": "AI已办结",
    "createdAt": "2026-06-05 10:20:00"
  }
}
```

---

### 2.4 人工审核提交

运营在工单详情弹窗中提交人工审核结果。

```
POST /api/after-sale/manual-audit
```

**Request Body：**

```json
{
  "ticketNo": "SH20260605002",
  "manualResult": 1,
  "manualRemark": "用户提供凭证清晰显示商品问题，符合售后政策，同意退款处理"
}
```

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `ticketNo` | string | 是 | 工单号 |
| `manualResult` | int | 是 | 审核结果：1同意售后 2驳回售后 |
| `manualRemark` | string | 否 | 人工备注（提交后自动入库为优质判例） |

**Response：**

```json
{
  "code": 200,
  "message": "审核提交成功，优质判例已自动入库",
  "data": {
    "ticketNo": "SH20260605002",
    "ticketStatus": 1,
    "ticketStatusDesc": "已办结",
    "caseGenerated": true
  }
}
```

---

### 2.5 批量指派客服

```
POST /api/after-sale/batch-assign
```

**Request Body：**

```json
{
  "ticketNos": ["SH20260605002", "SH20260604008", "SH20260604015"],
  "assignee": "张三"
}
```

**Response：**

```json
{
  "code": 200,
  "message": "成功指派 3 条工单给 张三",
  "data": {
    "successCount": 3,
    "failCount": 0
  }
}
```

---

### 2.6 批量重试工单

将「待AI审核」状态的工单重新投递 MQ 审核队列。

```
POST /api/after-sale/batch-retry
```

**Request Body：**

```json
{
  "ticketNos": ["SH20260605003", "SH20260605004"]
}
```

**Response：**

```json
{
  "code": 200,
  "message": "成功重试 2 条工单",
  "data": {
    "successCount": 2,
    "failCount": 0
  }
}
```

---

### 2.7 导出工单 Excel

```
GET /api/after-sale/export
```

Query 参数与 2.2 列表查询一致，额外参数：

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `fields` | string | 否 | 导出字段，逗号分隔，默认全部 |

**Response：** 返回 Excel 文件（`Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`），HTTP 200。

---

## 三、AI知识库接口

### 3.1 文档列表查询（分页）

```
GET /api/knowledge/list?page=1&size=20
```

**Query Parameters：**

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `page` | int | 否 | 默认1 |
| `size` | int | 否 | 默认20 |
| `docName` | string | 否 | 文档名称模糊搜索 |
| `category` | string | 否 | 文档分类筛选 |
| `vectorizeStatus` | int | 否 | 向量化状态：0待解析 1解析中 2已向量化 3失败 |

**Response：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "docId": 1,
        "docCode": "DOC001",
        "docName": "平台7天无理由售后规则.pdf",
        "category": "platform_general",
        "categoryDesc": "平台通用规则",
        "fileType": "pdf",
        "fileSize": "1.2 MB",
        "chunkCount": 28,
        "vectorizeStatus": 2,
        "vectorizeStatusDesc": "已向量化",
        "uploadedBy": "管理员",
        "uploadedAt": "2026-05-20 10:00:00"
      }
    ],
    "total": 5,
    "page": 1,
    "size": 20,
    "pages": 1
  }
}
```

---

### 3.2 上传知识库文档

异步解析，提交后返回「已进入解析队列」。

```
POST /api/knowledge/upload
```

**Request：** `multipart/form-data`

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `file` | file | 是 | PDF/Word/TXT，最大20MB |
| `category` | string | 是 | 分类：platform_general / digital / fresh / history_case |

**Response：**

```json
{
  "code": 200,
  "message": "文档上传成功，已进入异步解析向量化队列",
  "data": {
    "docId": 6,
    "docCode": "DOC006",
    "docName": "生鲜食品售后标准流程.pdf",
    "vectorizeStatus": 0,
    "vectorizeStatusDesc": "待解析"
  }
}
```

---

### 3.3 文档切片详情

查看文档被拆分后的所有 Chunk。

```
GET /api/knowledge/chunks/{docId}?page=1&size=50
```

**Path Parameters：**

| 参数名 | 类型 | 说明 |
|---|---|---|
| `docId` | long | 文档ID |

**Response：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "docId": 1,
    "docName": "平台7天无理由售后规则.pdf",
    "totalChunks": 28,
    "records": [
      {
        "chunkId": 1,
        "chunkIndex": 0,
        "chunkText": "第一章 总则 1.1 为保障消费者权益，明确平台售后服务标准，特制定本规则。1.2 本规则适用于平台内所有入驻商家及消费者...",
        "tokenCount": 128
      },
      {
        "chunkId": 5,
        "chunkIndex": 4,
        "chunkText": "第3章 售后处理流程 3.1 消费者在签收商品之日起7日内...",
        "tokenCount": 135
      }
    ]
  }
}
```

---

### 3.4 重新向量化文档

```
POST /api/knowledge/re-vectorize/{docId}
```

**Response：**

```json
{
  "code": 200,
  "message": "文档已重新进入解析向量化队列",
  "data": {
    "docId": 1
  }
}
```

---

### 3.5 删除文档

```
DELETE /api/knowledge/{docId}
```

**Response：**

```json
{
  "code": 200,
  "message": "文档及其切片已删除"
}
```

---

## 四、MQ死信运维接口

### 4.1 死信消息列表（分页）

```
GET /api/dlq/list?page=1&size=20
```

**Query Parameters：**

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `page` | int | 否 | 默认1 |
| `size` | int | 否 | 默认20 |
| `startTime` | string | 否 | 异常时间起始 |
| `endTime` | string | 否 | 异常时间结束 |
| `ticketNo` | string | 否 | 关联工单号 |
| `errorReason` | string | 否 | 异常原因模糊搜索 |
| `dlqStatus` | int | 否 | 状态：0待处理 1已重试 2已删除 |

**Response：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "msgId": "MSG10001",
        "queueName": "queue.refund.only",
        "exchangeName": "exchange.after.sale",
        "routingKey": "refund.only.created",
        "ticketNo": "SH20260605001",
        "errorReason": "大模型Ollama调用超时",
        "retryCount": 3,
        "maxRetry": 3,
        "dlqStatus": 0,
        "dlqStatusDesc": "待处理",
        "errorTime": "2026-06-05 10:20:15",
        "lastRetryTime": null
      }
    ],
    "total": 17,
    "page": 1,
    "size": 20,
    "pages": 1
  }
}
```

---

### 4.2 死信消息重试

```
POST /api/dlq/retry/{id}
```

**Path Parameters：**

| 参数名 | 类型 | 说明 |
|---|---|---|
| `id` | long | 死信记录ID |

**Response：**

```json
{
  "code": 200,
  "message": "消息已重新投递到原队列",
  "data": {
    "msgId": "MSG10001",
    "targetQueue": "queue.refund.only"
  }
}
```

---

### 4.3 死信批量重试

```
POST /api/dlq/batch-retry
```

**Request Body：**

```json
{
  "ids": [1, 2, 3, 5, 8]
}
```

**Response：**

```json
{
  "code": 200,
  "message": "批量重试完成",
  "data": {
    "totalCount": 5,
    "successCount": 5,
    "failCount": 0,
    "failDetails": []
  }
}
```

---

### 4.4 删除死信消息

```
DELETE /api/dlq/{id}
```

**Response：**

```json
{
  "code": 200,
  "message": "死信消息已删除"
}
```

---

## 五、数据仪表盘接口

### 5.1 获取仪表盘统计

```
GET /api/dashboard/stats
```

**Response：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "todayNewOrders": 1286,
    "todayAiCompleted": 892,
    "pendingManual": 143,
    "aiPassRate": 92.70,
    "todayDlqCount": 17,
    "todayNewOrdersTrend": 12.0,
    "todayAiCompletedTrend": 8.0,
    "pendingManualTrend": -5.0,
    "aiPassRateTrend": 2.3,
    "todayDlqCountTrend": -32.0,
    "statisticsTime": "2026-06-05 14:30:00"
  }
}
```

---

### 5.2 近7日趋势图

```
GET /api/dashboard/trend?days=7
```

**Query Parameters：**

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `days` | int | 否 | 天数，默认7 |

**Response：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "dates": ["2026-05-30", "2026-05-31", "2026-06-01", "2026-06-02", "2026-06-03", "2026-06-04", "2026-06-05"],
    "totalOrders": [168, 192, 145, 221, 198, 176, 212],
    "aiCompleted": [142, 165, 120, 198, 172, 155, 186],
    "manualPending": [26, 27, 25, 23, 26, 21, 26]
  }
}
```

---

### 5.3 售后类型占比

```
GET /api/dashboard/type-ratio
```

**Response：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "refundOnly": { "count": 4856, "ratio": 58.0 },
    "refundReturn": { "count": 2678, "ratio": 32.0 },
    "complaint": { "count": 918, "ratio": 10.0 }
  }
}
```

---

### 5.4 店铺售后 TOP10 排行

```
GET /api/dashboard/store-ranking
```

**Response：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "rankMonth": "202606",
    "list": [
      { "rank": 1, "storeId": 101, "storeName": "XX数码旗舰店", "orderCount": 168 },
      { "rank": 2, "storeId": 102, "storeName": "XX服饰旗舰店", "orderCount": 142 },
      { "rank": 3, "storeId": 103, "storeName": "XX生鲜专营店", "orderCount": 97 }
    ]
  }
}
```

---

## 六、数据统计接口

### 6.1 月度统计总览

```
GET /api/report/monthly-summary?month=202606
```

**Query Parameters：**

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `month` | string | 是 | 月份，格式 yyyyMM |

**Response：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "month": "202606",
    "totalOrders": 8452,
    "aiProcessed": 7299,
    "aiProcessRate": 86.30,
    "manualIntervention": 1153,
    "manualInterventionRate": 13.70,
    "approvedCount": 7124,
    "approvedRate": 84.30,
    "rejectedCount": 355,
    "rejectedRate": 4.20,
    "closedCount": 973,
    "closedRate": 11.50
  }
}
```

---

### 6.2 每日统计明细

```
GET /api/report/daily?month=202606
```

**Response：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "date": "2026-06-01",
        "totalOrders": 145,
        "aiCompleted": 120,
        "manualCompleted": 18,
        "rejected": 5,
        "closed": 2,
        "aiProcessRate": 82.76,
        "rejectedRate": 3.45
      }
    ]
  }
}
```

---

### 6.3 月度店铺排行

```
GET /api/report/store-ranking?month=202606&page=1&size=20
```

**Response：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "rank": 1,
        "storeName": "XX数码旗舰店",
        "orderCount": 168,
        "aiProcessRate": 91.20,
        "rejectedRate": 3.80,
        "trend": 5.0
      }
    ],
    "total": 50,
    "page": 1,
    "size": 20,
    "pages": 3
  }
}
```

---

### 6.4 通过率/驳回率趋势

```
GET /api/report/rate-trend?months=6
```

**Query Parameters：**

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `months` | int | 否 | 最近N个月，默认6 |

**Response：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "labels": ["2026-01", "2026-02", "2026-03", "2026-04", "2026-05", "2026-06"],
    "approveRate": [88.0, 87.0, 90.0, 91.0, 92.0, 93.0],
    "rejectRate": [6.5, 6.0, 5.2, 4.8, 4.5, 4.2]
  }
}
```

---

### 6.5 导出报表

```
GET /api/report/export?type=monthly&month=202606
```

**Query Parameters：**

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `type` | string | 是 | 报表类型：daily / monthly / store_ranking |
| `month` | string | 是 | 月份 yyyyMM |

**Response：** 返回 Excel 文件。

---

## 七、系统配置接口

### 7.1 查询所有配置（按分组）

```
GET /api/config/list?group=redis
```

**Query Parameters：**

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `group` | string | 否 | 配置分组，不传返回全部 |

**Response：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "redis": [
      {
        "id": 1,
        "configKey": "rate_limit.max_requests_per_min",
        "configValue": "5",
        "configDesc": "单用户每分钟最大售后提交次数",
        "valueType": "int",
        "sortOrder": 1
      },
      {
        "id": 2,
        "configKey": "rate_limit.token_bucket_capacity",
        "configValue": "100",
        "configDesc": "令牌桶容量",
        "valueType": "int",
        "sortOrder": 2
      }
    ],
    "ai_rag": [
      {
        "id": 4,
        "configKey": "ollama.base_url",
        "configValue": "http://localhost:11434",
        "configDesc": "Ollama大模型服务地址",
        "valueType": "string",
        "sortOrder": 1
      }
    ],
    "mq": []
  }
}
```

---

### 7.2 更新配置

```
PUT /api/config/update
```

**Request Body：**

```json
{
  "id": 1,
  "configValue": "10"
}
```

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `id` | long | 是 | 配置ID |
| `configValue` | string | 是 | 新配置值 |

**Response：**

```json
{
  "code": 200,
  "message": "配置更新成功，已实时生效",
  "data": {
    "id": 1,
    "configKey": "rate_limit.max_requests_per_min",
    "configValue": "10",
    "updatedAt": "2026-06-05 14:30:00"
  }
}
```

---

### 7.3 批量更新配置

```
PUT /api/config/batch-update
```

**Request Body：**

```json
{
  "configs": [
    { "id": 1, "configValue": "10" },
    { "id": 2, "configValue": "200" },
    { "id": 5, "configValue": "5" }
  ]
}
```

**Response：**

```json
{
  "code": 200,
  "message": "3 条配置更新成功",
  "data": {
    "successCount": 3,
    "failCount": 0
  }
}
```

---

### 7.4 恢复默认配置

```
POST /api/config/reset/{id}
```

**Response：**

```json
{
  "code": 200,
  "message": "配置已恢复为默认值"
}
```

---

## 八、基础数据接口

### 8.1 店铺列表（下拉选择用）

```
GET /api/basic/stores?keyword=数码
```

**Query Parameters：**

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `keyword` | string | 否 | 店铺名称关键词 |

**Response：**

```json
{
  "code": 200,
  "message": "success",
  "data": [
    { "id": 101, "storeName": "XX数码旗舰店", "storeCode": "S001" },
    { "id": 102, "storeName": "XX服饰旗舰店", "storeCode": "S002" }
  ]
}
```

---

### 8.2 订单查询

```
GET /api/basic/order/{orderNo}
```

**Response：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1001,
    "orderNo": "DD998765",
    "storeId": 101,
    "storeName": "XX数码旗舰店",
    "userId": 10086,
    "userName": "张三",
    "userPhone": "138****1234",
    "productName": "iPhone 15 Pro Max 256GB",
    "productSpec": "深黑色 / 256GB",
    "payAmount": 8999.00,
    "orderStatus": 3,
    "orderStatusDesc": "已签收",
    "orderTime": "2026-06-03 14:30:22"
  }
}
```

---

### 8.3 售后类型枚举

```
GET /api/basic/after-sale-types
```

**Response：**

```json
{
  "code": 200,
  "message": "success",
  "data": [
    { "code": 1, "name": "仅退款" },
    { "code": 2, "name": "退货退款" },
    { "code": 3, "name": "投诉" }
  ]
}
```

---

### 8.4 工单状态枚举

```
GET /api/basic/ticket-statuses
```

**Response：**

```json
{
  "code": 200,
  "message": "success",
  "data": [
    { "code": 0, "name": "待AI审核" },
    { "code": 1, "name": "AI已办结" },
    { "code": 2, "name": "待人工审核" },
    { "code": 3, "name": "已驳回" },
    { "code": 4, "name": "已关闭" }
  ]
}
```

---

## 九、操作日志接口

### 9.1 操作日志列表

```
GET /api/log/list?page=1&size=20
```

**Query Parameters：**

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `page` | int | 否 | 默认1 |
| `size` | int | 否 | 默认20 |
| `bizType` | string | 否 | 业务类型筛选 |
| `bizId` | string | 否 | 业务ID筛选 |

**Response：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "bizType": "order_audit",
        "bizTypeDesc": "工单审核",
        "bizId": "SH20260605002",
        "operator": "管理员",
        "action": "人工审核 - 同意售后",
        "detail": "{\\\"manualResult\\\":1,\\\"manualRemark\\\":\\\"符合政策，同意退款\\\"}",
        "ipAddress": "192.168.1.100",
        "createdAt": "2026-06-05 14:00:00"
      }
    ],
    "total": 1280,
    "page": 1,
    "size": 20,
    "pages": 64
  }
}
```

---

> **附录：API 汇总表**

| # | 模块 | 方法 | 路径 | 说明 |
|---|---|---|---|---|
| 1 | 工单 | POST | `/api/after-sale/submit` | 提交售后申请 |
| 2 | 工单 | GET | `/api/after-sale/list` | 工单列表（分页） |
| 3 | 工单 | GET | `/api/after-sale/detail/{ticketNo}` | 工单详情 |
| 4 | 工单 | POST | `/api/after-sale/manual-audit` | 人工审核提交 |
| 5 | 工单 | POST | `/api/after-sale/batch-assign` | 批量指派客服 |
| 6 | 工单 | POST | `/api/after-sale/batch-retry` | 批量重试工单 |
| 7 | 工单 | GET | `/api/after-sale/export` | 导出Excel |
| 8 | 知识库 | GET | `/api/knowledge/list` | 文档列表（分页） |
| 9 | 知识库 | POST | `/api/knowledge/upload` | 上传文档 |
| 10 | 知识库 | GET | `/api/knowledge/chunks/{docId}` | 切片详情 |
| 11 | 知识库 | POST | `/api/knowledge/re-vectorize/{docId}` | 重新向量化 |
| 12 | 知识库 | DELETE | `/api/knowledge/{docId}` | 删除文档 |
| 13 | 死信 | GET | `/api/dlq/list` | 死信列表（分页） |
| 14 | 死信 | POST | `/api/dlq/retry/{id}` | 单条重试 |
| 15 | 死信 | POST | `/api/dlq/batch-retry` | 批量重试 |
| 16 | 死信 | DELETE | `/api/dlq/{id}` | 删除死信 |
| 17 | 仪表盘 | GET | `/api/dashboard/stats` | 仪表盘统计 |
| 18 | 仪表盘 | GET | `/api/dashboard/trend` | 近7日趋势 |
| 19 | 仪表盘 | GET | `/api/dashboard/type-ratio` | 类型占比 |
| 20 | 仪表盘 | GET | `/api/dashboard/store-ranking` | 店铺TOP10 |
| 21 | 统计 | GET | `/api/report/monthly-summary` | 月度总览 |
| 22 | 统计 | GET | `/api/report/daily` | 每日明细 |
| 23 | 统计 | GET | `/api/report/store-ranking` | 店铺排行 |
| 24 | 统计 | GET | `/api/report/rate-trend` | 通过率趋势 |
| 25 | 统计 | GET | `/api/report/export` | 导出报表 |
| 26 | 配置 | GET | `/api/config/list` | 配置列表 |
| 27 | 配置 | PUT | `/api/config/update` | 更新配置 |
| 28 | 配置 | PUT | `/api/config/batch-update` | 批量更新 |
| 29 | 配置 | POST | `/api/config/reset/{id}` | 恢复默认 |
| 30 | 基础 | GET | `/api/basic/stores` | 店铺列表 |
| 31 | 基础 | GET | `/api/basic/order/{orderNo}` | 订单查询 |
| 32 | 基础 | GET | `/api/basic/after-sale-types` | 售后类型枚举 |
| 33 | 基础 | GET | `/api/basic/ticket-statuses` | 工单状态枚举 |
| 34 | 日志 | GET | `/api/log/list` | 操作日志 |
